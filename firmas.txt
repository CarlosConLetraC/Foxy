├── f_include <------------------- ruta usada por foxy-lang para buscar módulos/submódulos dinámicos.
│   ├── bit32 <------------------- módulo foxy-lang "bit32" (include "bit32") para operaciones manipuladoras a nivel de bits de 32 bits.
│   │   ├── byteswap.c <---------- función nativa en C para invertir el orden de los bytes (endianness).
│   │   ├── countlz.c <----------- función nativa en C para contar ceros a la izquierda (leading zeros).
│   │   ├── countrz.c <----------- función nativa en C para contar ceros a la derecha (trailing zeros).
│   │   ├── extract.c <----------- función nativa en C para extraer un rango de bits.
│   │   ├── init.c <-------------+ cuerpo de firmas bit32/init.h que cargan al entorno foxy-lang un dict.
│   │   │                        └ dict bit32 = { byteswap = <function at 0x101>, countlz = <function at 0x102>, ... }
│   │   ├── init.h <-------------- firmas de bit32/init.h para trabajar mediante f_lib.h | f_lib.c.
│   │   ├── lrotate.c <----------- función nativa en C para rotar bits a la izquierda.
│   │   ├── replace.c <----------- función nativa en C para reemplazar un rango de bits.
│   │   └── rrotate.c <----------- función nativa en C para rotar bits a la derecha.
│   ├── math <-------------------- módulo de funciones matemáticas nativas.
│   │   ├── abs.c <--------------- función nativa en C para valor absoluto.
│   │   ├── acos.c <-------------- función nativa en C para arcocoseno.
│   │   ├── asin.c <-------------- función nativa en C para arcoseno.
│   │   ├── atan2.c <------------- función nativa en C para arcotangente de dos variables.
│   │   ├── atan.c <-------------- función nativa en C para arcotangente.
│   │   ├── ceil.c <-------------- función nativa en C para redondeo hacia arriba.
│   │   ├── clamp.c <------------- función nativa en C para limitar un valor dentro de un rango [min, max].
│   │   ├── cos.c <--------------- función nativa en C para coseno.
│   │   ├── cosh.c <-------------- función nativa en C para coseno hiperbólico.
│   │   ├── deg.c <--------------- función nativa en C para convertir radianes a grados.
│   │   ├── exp.c <--------------- función nativa en C para función exponencial e^x.
│   │   ├── floor.c <------------- función nativa en C para redondeo hacia abajo.
│   │   ├── fmod.c <-------------- función nativa en C para residuo de división flotante.
│   │   ├── frexp.c <------------- función nativa en C para descomponer flotantes en mantisa y exponente.
│   │   ├── init.c <-------------+ cuerpo de firmas math/init.h que cargan al entorno foxy-lang un dict.
│   │   │                        └ dict math = { abs = <function at 0x105>, cos = <function at 0x106>, pi = 3.14159, ... }
│   │   ├── init.h <-------------- firmas de math/init.h para trabajar mediante f_lib.h | f_lib.c.
│   │   ├── ldexp.c <------------- función nativa en C para multiplicar un flotante por dos elevado a una potencia.
│   │   ├── log10.c <------------- función nativa en C para logaritmo base 10.
│   │   ├── log.c <--------------- función nativa en C para logaritmo natural.
│   │   ├── max.c <--------------- función nativa en C para obtener el valor máximo.
│   │   ├── min.c <--------------- función nativa en C para obtener el valor mínimo.
│   │   ├── modf.c <-------------- función nativa en C para separar parte entera y fraccionaria.
│   │   ├── perlin.c <------------ función nativa en C para generación de ruido Perlin 1D/2D.
│   │   ├── perlin_noise.c <------ implementación algorítmica de soporte para ruido Perlin.
│   │   ├── pi.h <---------------- constante nativa en C cargado al entorno foxy.
│   │   ├── pow.c <--------------- función nativa en C para potenciación.
│   │   ├── rad.c <--------------- función nativa en C para convertir grados a radianes.
│   │   ├── random.c <------------ función nativa en C para generación de números pseudoaleatorios.
│   │   ├── randomseed.c <-------- función nativa en C para establecer la semilla pseudoaleatoria.
│   │   ├── sin.c <--------------- función nativa en C para seno.
│   │   ├── sinh.c <-------------- función nativa en C para seno hiperbólico.
│   │   ├── sqrt.c <-------------- función nativa en C para raíz cuadrada.
│   │   ├── tan.c <--------------- función nativa en C para tangente.
│   │   ├── tanh.c <-------------- función nativa en C para tangente hiperbólica.
│   │   └── xclamp.c <------------ función nativa en C para envolver cíclicamente un valor dentro de un rango [min, max].
│   └── sys <--------------------- módulo foxy-lang "sys" (include "sys").
│       ├── file <---------------- submódulo foxy-lang "file" (include "sys/file").
│       │   ├── close.c <--------- función nativa en C cargado al entorno foxy.
│       │   ├── flush.c <--------- función nativa en C cargado al entorno foxy.
│       │   ├── init.c <---------+ cuerpo de firmas sys/file/init.h que cargan al entorno foxy-lang un dict. 
│       │   │                    └ dict file = { close = <function at 0x117>, flush = <function at 0x118>, lines = <function at 0x119>, read = <function at 0x120> }
│       │   ├── init.h <---------- firmas de sys/file/init.h para trabajar mediante f_lib.h | f_lib.c.
│       │   ├── lines.c <--------- función nativa en C cargado al entorno foxy.
│       │   ├── read.c <---------- función nativa en C cargado al entorno foxy.
│       │   ├── seek.c <---------- función nativa en C cargado al entorno foxy.
│       │   └── write.c <--------- función nativa en C cargado al entorno foxy.
│       ├── in <------------------ submódulo foxy-lang "in" para entrada estándar (include "sys/in").
│       │   ├── init.c <---------+ cuerpo de firmas sys/in/init.h que cargan al entorno foxy-lang un dict.
│       │   │                    └ dict in = { read = <function at 0x121>, readline = <function at 0x122> }
│       │   ├── init.h <---------- firmas de sys/in/init.h para trabajar mediante f_lib.h | f_lib.c.
│       │   ├── read.c <---------- función nativa en C para lectura de búfer desde stdin.
│       │   └── readline.c <------ función nativa en C para lectura de líneas completas desde stdin.
│       ├── init.c <-------------+ cuerpo de firmas sys/init.h que carga al entorno foxy-lang un dict.
│       │                        └ dict sys = { out = (dict){ print = <function at 0x123>, printf = <function at 0x124>, println = <function at 0x125> } }
│       ├── init.h <-------------- firmas de sys/init.h para trabajar mediante f_lib.h | f_lib.c.
│       └── out <----------------- submódulo foxy-lang "out" (include "sys/out").
│           ├── init.c <---------+ cuerpo de firmas sys/out/init.h que cargan al entorno foxy-lang un dict.
│           │                    └ dict out = { print = <function at 0x123>, printf = <function at 0x124>, println = <function at 0x125> }
│           ├── init.h <---------- firmas de sys/out/init.h para trabajar mediante f_lib.h | f_lib.c
│           ├── print.c <--------- función nativa en C cargado al entorno foxy.
│           ├── printf.c <-------- función nativa en C cargado al entorno foxy.
│           └── println.c <------- función nativa en C cargado al entorno foxy.
├── include
│   ├── f_array.h <--------------- firmas para manejar los arreglos de foxy, char letras[], int numeros_primos[], etc.
│   ├── f_ast.h <----------------- firmas para trabajar con el código fuente ya aprovado por f_lexer.h.
│   ├── f_callstack.h <----------+ firmas para poder crear instancias foxy-lang que permitan ejecutar distintos
│   │                            └ subprocesos y no dejar todo en un stack (subprocesos) y el stack principal.
│   ├── f_class.h <--------------- firmas para trabajar con tipos de dato class de foxy-lang.
│   ├── f_codegen.h <------------- firmas para generar el bytecode mediante f_foxmode.h. 
│   ├── f_constant.h <-----------+ firmas para poder guardar valores inmutables (ejemplo: println("Hola") @@ la
│   │                            ├ cadena de caracteres char[] se guarda en las constantes
│   │                            └ registradas por el lenguaje para optimización. @@)
│   ├── f_dict.h <---------------- firmas para trabajar con tipos de dato dict de foxy-lang.
│   ├── f_env.h <----------------- firmas para definir el entorno de foxy-lang (similar a _ENV de lua pero no lo mismo.)
│   ├── f_foxcode.h <------------- cabecera con lista maestra de foxcode (opcode).
│   ├── f_foxmode.h <------------- cabecera con lista maestra de instrucciones de 32 bits.
│   ├── f_function.h <------------ firmas para trabajar con tipos de dato function de foxy-lang.
│   ├── f_gc.h <------------------ firmas para utilizar garbage collector que limpian tipos de datos complejos (class, dict, class-object, function, struct, array)
│   ├── f_lexer.h <--------------- firmas para manejar y aprovar un archivo .foxy-lang.
│   ├── f_lib.h <----------------- firmas para cargar librerías nativas de C que trabajen junto a foxy-lang (math, sys/*, string, os, etc)
│   ├── f_methods.h <------------- firmas para construir los methods de un tipo de dato class de foxy-lang (incluidos los flagged-method (__new, __tostring, etc)).
│   ├── f_object.h <-------------- firmas para instanciar un objeto foxy-lang de un tipo de dato class de foxy-lang.
│   ├── f_parser.h <-------------- firmas para trabajar con el "cerebro gramatical" para construir el AST.
│   ├── f_process.h <------------- firmas para tranajar procesos internos que controlen la ejecución de una function/método/flagged-method.
│   ├── f_runtime.h <------------- firmas para despachar y ejecutar foxcode (opcode) mediante una capa de orquestación global en tiempo de ejecución.
│   ├── f_settings.h <-----------+ firmas para poder crear macros que definir la versión del compilador, valores máximos para f(x), 
│   │                            ├ rutas de entorno (necesarios para trabajar con f_lib.h) para saber dónde buscar librerías *.so
│   │                            └ para foxy-lang.
│   ├── f_status.h <-------------- firmas para poder definir el código de salida del programa, process, vm, etc.
│   ├── f_symtable.h <-----------+ firmas para poder usar representaciones de una entidad de símbolo relacional (Fila), tipo MySQL.
│   │                            ├ Así como MySQL utiliza este método para acelerar consultas, será utilizado para trabajar con bytecode
│   │                            ├ en memoria ram/disco duro y el manejo de variables, evitando sobrecargar con algoritmos pesados buscar
│   │                            └ dicha variable / valores inmutables.
│   ├── f_token.h <--------------- firmas para generar los tokens para f_lexer.h 
│   ├── f_utils.h <--------------+ firmas para poder utilizar funciones prácticas para trabajar en cualquier .h | .c (ejemplo: leer un archivo,
│   │                            └ compartir f_utils_syswrite con print.c, printf.c, println.c, etc.),
│   ├── f_value.h <--------------- firmas para poder crear una estructura que maneje los tipos de dato foxy-lang.
│   ├── f_vm.h <------------------ firmas para trabajar con la maquina virtual de foxy-lang.
│   └── uthash.h <---------------- header para crear hashmaps necesarios que optimicen la busqueda de valores en memoria.
├── Makefile <-------------------- configuraciones y procesos a seguir para compilar foxy-lang.
└── src
    ├── compiler <---------------- versión compilador de foxy-lang.
    │   └── main_fox.c <---------- compilador de foxy-lang (los binarios del lenguaje son desplegados como *.fox, ejemplo: ./saludar.fox)
    ├── core <-------------------- cuerpo de otras firmas f_*.c para optimizar el tipado de interpreter/compiler.
    └── interpreter <------------- versión interprete de foxy-lang.
        ├── f_vm.c <-------------- cuerpo de f_vm.c que inicia las firmas de su header.
        └── main_foxy.c <--------- interprete de foxy-lang.

NOTA: si desea crear su propio módulo C enlazado a foxy-lang, evite los nombres "init.h" e "init.c".
Ya que foxy-lang reserva estos nombres para orquestar la simbología de librerías.